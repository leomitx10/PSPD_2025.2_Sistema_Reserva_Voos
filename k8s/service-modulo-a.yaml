apiVersion: v1
kind: Service
metadata:
  name: voos-service
  labels:
    app: voos-service
spec:
  type: ClusterIP
  ports:
  - port: 50051
    targetPort: 50051
    protocol: TCP
    name: grpc
  selector:
    app: voos-service
