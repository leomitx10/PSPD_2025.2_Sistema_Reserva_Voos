apiVersion: v1
kind: Service
metadata:
  name: hoteis-service
  labels:
    app: hoteis-service
spec:
  type: ClusterIP
  ports:
  - port: 50052
    targetPort: 50052
    protocol: TCP
    name: grpc
  selector:
    app: hoteis-service
