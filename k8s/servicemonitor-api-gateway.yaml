apiVersion: v1
kind: Service
metadata:
  name: api-gateway-metrics
  labels:
    app: api-gateway
    monitoring: "true"
spec:
  selector:
    app: api-gateway
  ports:
  - name: metrics
    port: 9090
    targetPort: 9090
    protocol: TCP
  type: ClusterIP
